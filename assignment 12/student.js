/*2. Write a program in JavaScript to map the student ids, names and scores.
a) Add data for 3 students (use map functions)
b) Get Student Names using map functions
c) Delete Student Scores using map functionsd) Display 1 parameter (only value), 2 parameters (value + key), and 3
parameters (value + key + map) for the student*/
    var student = [{
        id: 1,
        name: "John",
        score:78
      },
      {
        id: 2,
        name: "ron",
        score:80
      },
      {
        id: 3,
        name: "harry",
      score:90
      }
    ];
    Object.values(student).map(function(values){});
   let current = fluent(next.values).map(student => student.name).toArray();
    console.log(student);
    console.log(current[next.key]);